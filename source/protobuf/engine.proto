syntax = "proto3";

service Engine {
    rpc PutTrainImage(Image) returns (ID) {}
    rpc GetTrainImage(ID) returns (Image) {}
}

message ID {
    bytes bytes = 1;
}

message Image {
    ID id = 1;
}
